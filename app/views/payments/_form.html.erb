<%= simple_form_for(@payment) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :type %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
