<%= simple_form_for(@nation) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :iso_code %>
    <%= f.input :cee %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
